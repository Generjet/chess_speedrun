<!DOCTYPE html>
<html>
  <head>
    <title>Chess Speedrun</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-black text-white">
    <nav class="bg-green-500 p-4">
      <div class="container mx-auto flex justify-between items-center">
        <span class="text-xl font-semibold">Chess Speedrun</span>
        <div>
          <% if user_signed_in? %>
            <%= link_to 'Users', users_path, class: 'px-2' %>
            <%= link_to 'Inventories', inventories_path, class: 'px-2' %>
            <%= link_to 'Soldiers', chess_soldiers_path, class: 'px-2' %>
            <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'px-2' %>
          <% else %>
            <%= link_to 'Sign In', new_user_session_path, class: 'px-2' %>
            <%= link_to 'Register', new_user_registration_path, class: 'px-2' %>
          <% end %>
        </div>
      </div>
    </nav>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div class="container mx-auto mt-8">
      <%= yield %>
    </div>
  </body>
</html>
